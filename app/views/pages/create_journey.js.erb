// Ajoute la carte à la liste
var card = document.getElementById('journey-cart');
card.insertAdjacentHTML("beforeend", '<%= j(render 'components/card-list', business: @business) %>');

// Supprime le label "Your list of shops is empty"
if(document.getElementById('empty-label')) {
  document.getElementById('empty-label').remove();
}
